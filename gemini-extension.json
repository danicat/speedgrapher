{
  "name": "speedgrapher",
  "version": "0.6.0",
  "description": "A local MCP server for writers to streamline the writing process.",
  "mcpServers": {
    "speedgrapher": {
      "command": "${extensionPath}/speedgrapher"
    }
  }
}

